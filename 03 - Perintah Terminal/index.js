// - mkdir = membuat folder ex = mkdir testing
// - mkdir = multiple folder = mkdir css js html
// - mkdir = klo ada namanya spasi = mkdir Sigma\ Nations
// - mkdir = folder inside folder = mkdir assets/css

// - 

// - cd = masuk ke folder ex = cd Downloads
// - cd.. = keluar dari folder
// - cd / = langsung ke folder utama
// - cd ../../ProgramData = melewati 2 folder dan masuk ke folder yang dipilih atau diminta 

// - touch = membuat file ex = index.html App.js
// - touch = sembari didalam file = touch src/index.js
// - touch = multiple file = touch src/index.js app.js style.css 

// - rm = menghapus file = rm nama_file.ekstensi
// - rm = menghapus semua file = rm *
// rm bisa multiple juga

// - rm -rf nama_folder (rf remove folder)

// - code . = masuk ke vscode
// - ctrl + c = memberhentikan kode yang sedang berjalan

// ls = menampilakan isi folder
// ls . = menampilkan yang hidden juga dengan tambah titik
// clear atau cls = membersihkan 
// pwd = kamu sedang membuka path folder apa ?


// ls --help : menampilkan semua yang bisa kita gunakan dengan command ls

// -s size









// Perintah ls digunakan di sistem berbasis Unix/Linux untuk menampilkan daftar file dan direktori dalam suatu folder. Opsi -la adalah kombinasi dari dua opsi, yaitu -l dan -a, yang memberikan informasi tambahan dalam outputnya.

// Penjelasan Opsi -l dan -a:
// -l (long listing format):

// Menampilkan daftar file dalam format yang lebih detail (long format).
// Informasi yang ditampilkan meliputi:
// Izin file (permissions)
// Jumlah link (links)
// Pemilik file (owner)
// Grup file
// Ukuran file
// Waktu terakhir file dimodifikasi
// Nama file atau direktori
// -a (all files):

// Menampilkan semua file, termasuk file yang tersembunyi.
// File tersembunyi biasanya diawali dengan tanda titik (.), seperti .bashrc atau .gitignore.
// Secara default, file tersembunyi tidak ditampilkan oleh ls.
// Kombinasi -la:
// Ketika digabungkan menjadi ls -la, perintah ini:

// Menampilkan semua file, termasuk file tersembunyi.
// Menampilkan daftar dalam format panjang (dengan detail).